# ProductLift 学习笔记

> 这是 mentor(Claude)+ mentee 带做过程的学习笔记:记录每个里程碑做了什么、
> 关键发现、结果数字、踩过的坑,以及面试时该怎么讲。
> 约定:**每完成一个里程碑就更新本文件**;所有数字必须来自实际运行输出,不凭记忆。

---

## 进度总览

| 里程碑 | 内容 | 状态 | commit | 测试 |
|---|---|---|---|---|
| M0 | 环境 + EDA | ✅ 2026-07-05 | —(无代码) | — |
| M1 | 时间切分 + 标签定义 | ✅ 2026-07-05 | `71473e8` | 4 绿 |
| M2 | 特征工程 ⭐ | ✅ 2026-07-06 | `602fbbc` | 10 绿 |
| M3 | 建模与验证 ⭐ | ✅ 2026-07-06 | `1f662d7` | 4 绿 |
| M4 | 评估(Brier/ECE/recall@k/NDCG) | ✅ 2026-07-06 | `af11268` | 5 绿 |
| M5 | 实验与统计 ⭐ | ✅ 2026-07-06 | *待提交* | 7 绿 |
| M6 | 因果推断 ⭐ | ⬜ 下一个 | | |
| M7 | 漂移监控 | ⬜ | | |
| M8 | 上线打磨 | ⬜ | | |

测试进度:重启基线 1 绿 → 当前 **30 绿**(其余 7 个是 M6/M7 的待做练习)。

---

## 序章:推倒重来(2026-07-05)

上一轮做到 M3 但**从未 commit**,一次 git 工作树复位把 tracked 文件的改动全部抹掉。
用户决定不恢复,从自己的 `init` commit 重新开始;残留的旧产物(模型、特征表)
备份后清除,数据集和 Python 环境保留。

**教训(本项目最贵的一课):每个里程碑完成就 commit。**
未提交的工作 = 一条命令就能蒸发的工作。

---

## M0 — 环境与 EDA(2026-07-05)

**环境**:Python 3.12.11,pandas 3.0.3 / sklearn 1.9.0 / lightgbm 4.6.0。
`make` 不在 PATH → 直接用底层命令(`pytest` / `python -m scripts.xxx`)。

**数据长什么样**(Olist,一行 = 一个已送达订单里的一个商品):

- 110,197 行订单明细,32,216 个商品,71 个品类,时间跨度 2016-09 → 2018-08
- **55% 的商品只卖出过 1 单**,≥5 单的只有 14.7% —— 长尾/冷启动是这个数据集的底色
- 价格中位数 74.9、p99=887(重右偏);评分中位数 5(好评堆积)

**信号侦察**(全周期预览标签,q=0.25、min_orders=5 → 4,067 个合格商品,25.7% 正类):

| 特征(按类均值) | 健康 | 差生 | 结论 |
|---|---|---|---|
| price_vs_category_median | 1.93 | 0.51 | **最强分离器** |
| avg_price | 145.0 | 39.8 | 强,但半机械(revenue=价×量) |
| desc_len | 848 | 676 | 中等,业务可干预 |
| bad_review_rate | 0.142 | 0.138 | **几乎无信号(反直觉!)** |
| late_rate | 0.081 | 0.068 | 同上 |

**M0 最大发现**:差评/迟到与"品类内卖得垫底"几乎无关——卖不动主要是
定价与内容问题,不是口碑问题。面试时主动讲"我最初以为差评是主信号,数据否了它"。

---

## M1 — 时间切分 + 标签定义(2026-07-05,`71473e8`)

**做了什么**:`app/data/splits.py::temporal_split` + `app/data/labels.py::label_underperforming`。

**核心思想**:
- 时间序列必须**时间切分**(训练在过去、测试在未来)——随机切分让模型"看见未来",
  离线指标虚高、上线即崩。边界规则 `train ≤ T1 < valid ≤ T2 < test`,等号只在一侧,
  任何行恰好落进一个窗口。
- 标签 = **品类内**收入垫底 25%(和同类比,不是电子产品 vs 圆珠笔),
  窗口内不满 5 单的不评判;**先过滤再算分位线**(不让稀疏商品拖低品类的及格线)。
- 特征只用切分日**之前**的数据,标签只用**之后**的——这是全项目的泄漏防线。

**真实数据结果**:行级三窗 66.5% / 20.6% / 12.9%;
outcome 窗(2018-03-31 后 180 天)标出 **1,442 个商品,26.6% 差生**。

**坑**:
- `pd.Timestamp("2018-03-31")` = 当日 **00:00:00** → "train_end=03-31" 实际含到 03-30 深夜
- NaT 和任何比较都是 False → 含 NaT 的行会从所有窗口**静默消失**,必须 fail-loud

---

## M2 — 特征工程 ⭐(2026-07-06,`602fbbc`)

**做了什么**:`product / behavioral / temporal / encoders` 四个构建器 + `assemble` 集成。

**特征清单**(26 个特征,全部"截至预测日可知"):
- **内容/静态**:照片数、描述长度、名称长度、重量、体积、密度、log 变换、
  均价、**品类相对价格**(冷启动商品也有这些)
- **行为**:订单数、客户数、均价、运费、评分、差评率、迟到率、配送天数
- **时间**:上架时长、距最近一单天数、近 7/30/90 天订单数、30 天动量趋势

**关键设计决定**:
1. `bad_review_rate` 分母 = **有评价的订单**(把无评价当"非差评"会让稀疏商品显得健康)
2. 无评价 → **留 NaN**(缺失是信号;填补是 M3 Pipeline 的事,必须 fit-on-train)
3. `temporal_features` 第一行就**拒绝** as_of 之后的数据(ValueError)——泄漏摁死在源头
4. 趋势分母为零 → NaN(没有基数的加速度是未定义的,填 0/inf 都是编造信号)
5. **目标编码器**:`encoding(c) = (n_c·mean_c + m·global) / (n_c + m)`——样本越少越向
   全局均值收缩(贝叶斯平滑);**未见过的品类精确返回全局先验**;fit 只碰 train
6. `assemble` 两个 join 决定:label 侧**只放行 is_underperforming**
   (outcome 窗的 revenue/n_orders 进特征 = 把答案抄进考卷);inner join
   ("没被评判"≠"健康",1,442 个有标签商品中 558 个无特征窗历史,排除)

**产出**:`modeling_matrix.parquet` = **884 商品 × 28 列,24.0% 正类**;
唯一 NaN 列是 trend_30d(353 个,by design)。

---

## M3 — 建模与验证 ⭐(2026-07-06)

**做了什么**:从零新建 `app/models/{baseline,gbdt,calibrate,registry}.py`,
重写 `scripts/train.py`,超参/切分比例进 `config/params.yaml`。

**结果(真实数据,PR-AUC)**:

| | valid (133) | test (177,只碰一次) |
|---|---|---|
| 瞎猜基准(=正类占比) | 0.188 | 0.243 |
| LogReg baseline | 0.624 | **0.740** |
| GBDT + 校准 | 0.656 | **0.748** |

**诚实解读**:baseline 远超瞎猜(特征有真信号);GBDT 只比 baseline 高 0.008 ——
**884 行小数据上复杂度尚未被 lift 证明**。不做 baseline 就会拿 0.748 吹"模型很好",
不知道 0.740 用可解释的线性模型就能拿到。这就是 baseline-first 纪律的价值。

**核心思想**:
- **四折切分** train/valid/calib/test(441/133/133/177):早停用 valid,
  **校准用模型从没见过的 calib 折**(在训练集上校准只是矫正过拟合的自信),
  test 只在最后碰一次
- 早停监控 `average_precision` —— **停在你汇报的指标上**(用 AUC 早停、汇报 PR-AUC
  = 悄悄优化错目标);best_iter=33 说明早停真的干活了
- 不平衡处理:GBDT 用 `scale_pos_weight=负/正`(在 train 折上算,不碰 holdout),
  baseline 用 `class_weight="balanced"`
- 校准用 sigmoid 不用 isotonic:校准折只有 133 行,阶梯函数会过拟合
- `product_id` 不进特征:标识符 = 背答案,对新商品毫无泛化
- ModelCard = 训练/服务契约:serving 按卡片的特征名组行,预处理锁在 Pipeline 里
  → 训练怎么变换,服务就怎么变换(train/serve skew 是静默故障的头号来源)

**serving 冒烟**:`Predictor.predict_one` 单商品打分 0.0483/不标记 ✅

---

## M4 — 评估(2026-07-06)

**做了什么**:`evaluation/metrics.py` 四个指标(Brier / ECE / recall@k / NDCG@k)+
`offline_eval.py::evaluate_predictions`(overall + 分段)+ 重写 `scripts/evaluate.py`
(改在**从未碰过的 test 折**上评估——脚手架原版在整个矩阵上算,等于用训练数据给自己打分)。

**真实结果(test 折,177 个商品,43 个正类)**:

| 指标 | 值 | 怎么读 |
|---|---|---|
| ROC-AUC | 0.899 | 排序能力好 |
| PR-AUC | 0.748 | 主指标(地板=正类占比 0.243) |
| Brier | 0.098 | 概率均方误差 |
| **ECE** | **0.040** | 模型说 30% 时,真实频率平均只差 4 个点 → **校准层干活了** |
| recall@10% | 0.326 | 见下 |

**recall@10% = 0.326 的业务解读**:审查预算只有 top 10%(18 个名额),抓住了
43 个真差生里的 14 个。注意**容量上限**:18 个名额最多抓 18/43 = 41.9%,
所以 0.326 对应 precision@18 = 14/18 = **78%** —— 名额几乎没浪费,是名额不够。
这就是"把模型翻译成业务决策"的操作点指标。

**分段报告的价值(一眼暴露盲区)**:

| 价格段 | PR-AUC | recall@10% | 结论 |
|---|---|---|---|
| price_low | 0.800 | 0.18 | 便宜商品:模型最有用 |
| price_mid | 0.671 | 0.56 | 中间价:也可用 |
| price_high | **0.111** | **0.00** | 贵价商品:**几乎没用**(该段正类极少,PR-AUC 地板≈占比) |

Headline 0.748 藏住了 price_high 段的无用——**这就是为什么模型报告必须分段**。

**设计决定**:
- 段内样本 < 30 直接跳过(十几行上的 AUC 是"穿着数字外衣的噪声")
- 单类段的 AUC/recall 报 None 不编数;Brier/ECE 仍有定义照报
- recall@k 无正类时返回 NaN(没有可召回的东西,0 和 1 都是撒谎)
- 评估用 `card.feature_names` 切列——ModelCard 契约的实战应用

---

## M5 — 实验与统计 ⭐(2026-07-06)

**做了什么**:`experimentation/power.py`(功效分析 + 天数换算)、`analyze.py`
(双比例 z 检验 + 置信区间 + guardrail 检查)、`segments.py`(分段效应 + BH 多重检验校正)。

**用 params.yaml 的实验配置算一笔真账**(baseline 转化率 3%、要检测 +5% 相对提升、α=0.05、power=0.80):

> 每组需要 **207,936** 个样本 —— 日流量每组 2,000 要跑 **104 天**,20,000 也要 11 天。

这就是功效分析的价值:**先算,再跑**。"检测 5% 的提升"听起来很谦虚,对 3% 的基线
就是天文数字样本量;老板要"下周出结果"时,你可以反问"那 MDE 只能放宽到多少"——
n、α、power、MDE 四个量锁死,定三个解第四个(MDE 在分母上平方:效应减半,样本量×4)。

**核心思想**:
- **检验用 pooled SE,置信区间用 unpooled SE**:检验是在 H0("两组同率")下算的,
  pooling 是原假设自己的假设;CI 描述的是真实的两率世界,不能 pool。
  senior 答案不是 "p<0.05",而是 "+20% 相对提升,95% CI [x, y],p=0.004,guardrail 无恶化"
- **至少跑满 7 天**:不满一周 = 效应和星期几混杂 + 新奇效应主导——这是判断,不是数学
- **guardrail 方向显式注册**(`HIGHER_IS_WORSE` 表):退货率升=坏,满意度升=好;
  没注册方向的指标直接报错——"猜方向"就是退货率恶化上线的方式
- **多重检验**:12 个分段各测 α=0.05,纯靠运气出"赢家"的概率 ≈46%。
  用 Benjamini-Hochberg 控 FDR(比 Bonferroni 保留更多功效,适合探索性分段扫描);
  同一段的校正 p 随家族变大而变严:s0 在 2 段家族里 0.120 → 10 段家族里 0.266

**额外收获:抓到脚手架测试的构造缺陷**(证据链完整):
`test_more_segments_is_stricter` 原断言比较两个**不同家族的最小**校正 p,
但 seed-0 fixture 的 10 段家族里恰好有一段原始 p=0.0020(比 2 段家族最小值小 50 倍)
—— 对任何正确实现都不可满足(BH: 0.0198 vs 0.1204;Bonferroni: 0.0198 vs 0.2118 都失败),
而原始 p 值被 `test_ab_analyze` 用 statsmodels 钉死,没有实现自由度。
修法:改测试为比较**共享段**(fixture 的 rng 重置使 s0/s1 两个家族数据逐行相同)——
这才是它注释里写的 "comparable raw p" 的本意。
教训:**测试是 spec,但 spec 也会有 bug;推翻它需要完整证据链,不是"我觉得"**。

---

## 踩坑记录(每个都值一道面试题)

| 坑 | 现象 | 修法 |
|---|---|---|
| 未提交就是没做 | 上一轮全部工作被一次 git 复位抹掉 | 每个里程碑 commit |
| gitignore 未锚定 | `models/` 把 `app/models/` 源码变成 git 隐形 | 改 `/models/`(锚定根目录) |
| 收集错误中断 pytest | `app/models` 缺失时裸 `pytest` 直接 Interrupted | 模块建成后消失;期间用 `--ignore` |
| NaT 静默丢行 | NaT 与任何比较都是 False | 切分前 fail-loud 检查 |
| sklearn 1.9 校准 API | `cv="prefit"` 已移除 | `FrozenEstimator` 包住已拟合模型 |
| 特征名 fit/predict 不一致 | 7 条 UserWarning,train/serve-skew 气味 | 编码器补 `get_feature_names_out` + `set_output("pandas")`,不用 filterwarnings 压 |
| Olist 自带拼写错误 | `product_description_lenght` | 照抄,别"好心"改名 |
| 脚手架类型 bug | `predictor.py` 返回 `object` 却调 `.predict_proba` | 改 `Any`(M3 前 mypy 查不到该文件) |
| 脚手架测试缺陷 | test_segments 比较不同家族的 min 校正 p,任何正确实现都过不了 | 逐段对照 statsmodels 证明后改测试比较共享段(见 M5 节) |

---

## 下一步

- **M6 因果推断** ⭐:`causal/{propensity,uplift,exposure_bias}.py`——
  倾向得分/IPW("送得快是否导致好评")、T-learner uplift(该干预谁)、
  曝光位置偏差("是产品差,还是只是排得靠后")——直击本项目的核心业务追问。
- 挂账:M3 stretch(Optuna 调参)、avg_price 与 avg_unit_price 去重。
